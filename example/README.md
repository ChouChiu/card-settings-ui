# card_settings_ui example

Run the adaptive Material 3 settings demo:

```sh
flutter run
```

Resize the window or rotate the device to see the category list switch between
compact navigation and the two-pane landscape layout. The Appearance page can
also simulate dynamic color and an OLED-black page background.
